#lang racket/base

(provide (all-defined-out))

(define rss-feed
  `("/feed.xml"
    "Feed to the site"))
(define wp
  `("/files/bitcoin.pdf"
    "Bitcoin: A Peer-to-Peer Electronic Cash System"))
(define source-code
  `("#"
    "Source code to this site"))

